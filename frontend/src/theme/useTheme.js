/**
 * useTheme Hook
 * 
 * Re-export of useTheme hook from ThemeProvider for convenience.
 * This allows importing the hook directly: import { useTheme } from '@/theme/useTheme'
 */

export { useTheme } from './ThemeProvider';
export default useTheme;
